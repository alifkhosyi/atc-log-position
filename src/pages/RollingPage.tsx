// ============================================================
// src/pages/RollingPage.tsx — Rolling Harian (MVP read-only)
// ──────────────────────────────────────────────────────────
// Step 2 stub — routing target untuk PAGES_CABANG/PAGES_ADMIN.
// Topbar + toolbar + grid + PDF export di-build di step 3-9.
//
// Refer ROLLING_HANDOFF.md (v1 MVP) untuk scope + AC.
// ============================================================

import React from "react"

export default function RollingPage() {
  return (
    <div style={{ padding: 32 }}>
      <h1 style={{ fontSize: 24, marginBottom: 8 }}>Rolling Harian</h1>
      <p style={{ color: "var(--text-muted)" }}>
        Implementasi MVP sedang berjalan. Cek lagi setelah step 3-10 selesai.
      </p>
    </div>
  )
}
